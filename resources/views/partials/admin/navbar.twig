<nav class="navbar navbar-expand-lg px-lg-4 navbar-light fixed-top bg-white py-2">
    <div class="container-fluid">
        <a class="navbar-brand border-end border-4 border-light me-3 pe-3  text-primary fw-bold" href="/">{{ get_var('APP_ABBRV') | upper }}</a>
        <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="bi bi-filter-right" style="font-size: 1.5em"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-3 mb-lg-0">
                {% if (is_('admin') and is_('approved')) or is_('super') %}
                    {% include "admin/components/admin-nav.twig" %}
                {% endif %}
            </ul>
            <ul class="navbar-nav ms-auto mb-3 mb-lg-0">
                <li class="nav-item border-end border-4 border-light me-3 pe-3">
                    <a class="nav-link {{ CURRENT_ROUTE == '/account'? 'text-primary fw-bold': '' }}" href="/account"><i class="bi bi-person fs-5 me-1"></i> Account</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/logout"><i class="bi bi-door-open fs-5 me-1"></i> Logout</a>
                </li>
            </ul>
        </div>
    </div>
</nav>
